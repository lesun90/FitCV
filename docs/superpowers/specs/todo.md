- dashboard: 
    - change button to edit and remove (remove open editor/ duplicate/ ...More)
    - Have feature to have a TRUE preview of resume instead of generic pics (how about generate a snapshot for after compile successfully and use it in dashboard)

- In editor:
    - For text editor, add option to making word bold/i/u and change color , should renderable too. Simple text field does not need that option
    - in Summary -> Profile highlights: redesign the UI, look too redundant.

The compile button sometime not work, even when auto compile can compile successfully