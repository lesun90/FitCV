/create-plan 
Revamp how layout work
Editing layout
- The summary is fixed. cannot be removed
- User add a  new section, edit the name (this will be the name show on CV) similar to command `\cvsection{WORK EXPERIENCE}`
- when edit a section, user can create a sub-section, select from a dropdown menu (similar to command `\begin{experience}, In each sub-section, user can add an entry, entry type is secled from a drop down menu, (cventry, 



http://localhost:5173/latexeditor
edit local, trigger recompile but not update